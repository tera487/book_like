<%= form_with(model: current_user.relationships.find_by(follow_id: @user.id),url: users_relationship_path(@user.id),method: :delete, local: false ) do |f| %>
  <%= f.hidden_field :follow_id, value:@user.id %>
  <%= f.submit 'フォロー中', class: 'btn back_button' %>
<% end %>