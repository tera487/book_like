<%= form_with(model: current_user.relationships.build, url: :users_relationships,local: false) do |f| %>
  <%= f.hidden_field :follow_id, value:@user.id %>
  <%= f.submit 'フォロー', class: 'btn back_button' %>
<% end %>